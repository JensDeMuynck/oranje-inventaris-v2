const state = () => ({
    // uid: '',
    // name: '',
})

const getters = {
    // getUid: state => {
    //   return state.uid
    // },
    // getName: state => {
    //   return state.name
    // }
}

const actions = {
	// setUid(context, uid) {
	// 	context.commit('setUid', uid);
	// },
	// setName(context, name) {
	// 	context.commit('setName', name);
	// },
}

const mutations = {
	// setUid(state, uid) {
	// 	state.uid = uid;
	// },
	// setName(state, name) {
	// 	state.name = name;
	// },
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}